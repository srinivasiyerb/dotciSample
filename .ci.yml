environment:
   language: java
   services:
      - rldevops/redis-server



build:
    run: echo hello world

notifications: #list of notifications, notified ONLY on build fail and branch recovery.
  - email: srinivasiyerb@gmail.com
